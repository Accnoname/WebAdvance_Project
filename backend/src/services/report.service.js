const Order = require('../models/Order.model');
const Payment = require('../models/Payment.model');

// TODO: Implement aggregation pipeline
const getRevenue = async (period) => { throw new Error('TODO'); };
const getBestSellers = async () => { throw new Error('TODO'); };
const getTableUsage = async () => { throw new Error('TODO'); };

module.exports = { getRevenue, getBestSellers, getTableUsage };
