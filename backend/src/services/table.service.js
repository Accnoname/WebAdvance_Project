const TableRepository = require('../repositories/table.repository');
const QRCode = require('qrcode');

// TODO: Implement
const getAll = async () => { throw new Error('TODO'); };
const create = async (data) => { throw new Error('TODO'); };
const updateStatus = async (id, status) => { throw new Error('TODO'); };

module.exports = { getAll, create, updateStatus };
